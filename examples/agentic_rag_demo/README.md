# Agentic RAG Demo

Use this example to try the rebuilt CardioMAS runtime:

```bash
cardiomas build-corpus --config examples/agentic_rag_demo/runtime.yaml
cardiomas query "What labels are present?" --config examples/agentic_rag_demo/runtime.yaml
```
